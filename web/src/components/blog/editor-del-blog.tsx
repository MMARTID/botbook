"use client";

import { makePage } from "@keystatic/next/ui/app";
import config from "../../../keystatic.config";

/** La interfaz de Keystatic (cliente) con nuestra configuración. */
export const EditorDelBlog = makePage(config);
