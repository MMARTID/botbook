# Contexto de BotBook para Claude Code

> Fuente: `AGENTS.md` (referencia técnica del proyecto). Este archivo es el resumen
> operativo que Claude Code debe cargar antes de tocar código o diseño.

## Descripción del Proyecto

**BotBook** es una plataforma **SaaS multi-tenant** que ofrece **recepcionistas de voz con IA**
para pequeños negocios en **España**.

- **Qué hace:** cada negocio dispone de uno o más agentes de voz que atienden las llamadas
  entrantes, responden preguntas, consultan el horario del negocio, comprueban disponibilidad
  y reservan citas directamente en el calendario de Google u Outlook del negocio.
- **Para quién:** peluquerías, barberías, salones de uñas, centros de estética y clínicas de
  fisioterapia (`businessType`: `peluqueria`, `barberia`, `salon-de-unas`,
  `centro-de-estetica`, `fisioterapia`, `other`).
- **Mercado:** España / Europa. Las cuentas europeas usan **Retell.ai** por defecto por
  cumplimiento **RGPD**; **Vapi** se mantiene para futura expansión fuera de Europa.
- **Modelo de negocio:** suscripción con Stripe y 7 días de prueba. Planes:
  - `inicio` — 69 €/mes, 100 min incluidos, 0,60 €/min extra
  - `pro` — 149 €/mes, 400 min incluidos, 0,45 €/min extra (destacado)
  - `scale` — 299 €/mes, 1000 min incluidos, 0,35 €/min extra
- **Idioma del código:** **todo el codebase está en español** — copy de UI, comentarios,
  nombres de variables y lógica de negocio. Mantén todo en español al modificar código o
  añadir texto visible para el usuario. El inglés solo se acepta para términos técnicos
  (`routes.ts`, `service.ts`, `prisma`, `fastify`…).

## Stack Técnico

### Backend

| Capa | Tecnología |
|------|------------|
| Runtime | Node.js 20, TypeScript 5.9, **ESM** (`"type": "module"`) |
| Framework HTTP | Fastify 5 |
| Base de datos | PostgreSQL 15 + Prisma ORM |
| Caché / Colas | Redis 7 (IORedis) + BullMQ |
| Voice AI | Vapi API + Retell.ai (`retell-sdk`) |
| LLM | Anthropic Claude — `claude-haiku-4-5` para clasificación de llamadas |
| Almacenamiento | Cloudflare R2 (S3-compatible, `@aws-sdk/client-s3`) |
| Telefonía | Twilio (compra de números, inbound/outbound) |
| Pagos | Stripe (Checkout Sessions embebido, Customer Portal, webhooks) |
| Calendario | Google Calendar API (`googleapis`), Microsoft Graph (Outlook) |
| Auth | JWT propio (`jsonwebtoken`, `@fastify/jwt`) + Google OAuth 2.0 + bcrypt |
| Validación | Zod 3 |
| Contenedores | Docker + Docker Compose (perfiles `dev` / `prod`) |
| Testing | Vitest 4, MSW, `@testcontainers/postgresql`, Playwright |
| Formato / Lint | Prettier + ESLint (`@typescript-eslint/recommended`) |

Puertos: backend `3000`, frontend `3001`, Prisma Studio `5555`, ngrok dashboard `4040`.

### Frontend

| Capa | Tecnología |
|------|------------|
| Framework | **Next.js 14** (App Router), React 18, TypeScript 5 |
| Styling | **Tailwind CSS v3.4** + variables CSS en `globals.css` + PostCSS |
| Estado servidor | TanStack Query v5 (`staleTime: 30s`, sin retry, sin refetch on focus) |
| Estado global | `BusinessProvider` (context): `business`, `isLoadingBusiness`, `hasToken`, `isError` |
| HTTP | Axios con interceptor de Bearer token |
| Iconos | **Lucide React** |
| Animación | Framer Motion 10 |
| Gráficas | Recharts 3 |
| Pagos | `@stripe/react-stripe-js` + `@stripe/stripe-js` (Embedded Checkout) |
| Demo de voz | `@vapi-ai/web` (la demo pública es **solo Vapi**) |
| Lint | `eslint-config-next` (`next/core-web-vitals`) |

- **Proxy:** `/api/backend/:path*` → `http://localhost:3000/:path*` (rewrites en `next.config.mjs`).
- **Redirects:** plurales a singulares (`/peluquerias` → `/peluqueria`).
- **Persistencia en cliente:** token en `localStorage` (`botbook_token`; legacy `token`, `jwt`),
  plan pendiente (`botbook_pending_plan`), país de registro
  (`botbook_registration_country`), contexto ROI en `localStorage` + `sessionStorage` con
  TTL de 1 hora.

> Nota: el `package.json` de la raíz lista `tailwindcss ^4.3.2` como dependencia del backend.
> Es una dependencia huérfana: el frontend usa **Tailwind 3.4.1**. No la tomes como referencia.

## Estructura del Monorepo

```
/
├── src/                    # Backend ESM TypeScript
│   ├── server.ts           # Entry point Fastify (registra rutas y workers)
│   ├── plugins/            # auth, CORS, rate-limit, multipart
│   ├── modules/            # Módulos de rutas por dominio
│   ├── adapters/           # Vapi, Retell, Twilio
│   ├── lib/                # Utilidades compartidas
│   ├── jobs/               # Workers BullMQ
│   └── config/             # Constantes de configuración
├── frontend/               # Next.js 14
│   ├── src/app/            # Páginas App Router
│   ├── src/components/     # Componentes React
│   ├── src/lib/            # Cliente API, tipos, helpers, SEO, ROI
│   └── src/hooks/          # Hooks personalizados
├── prisma/                 # Schema + migraciones
├── dist/                   # Salida compilada del backend (tsc)
├── scripts/                # Scripts puntuales (E2E)
├── tests/                  # Suite Vitest
├── Dockerfile              # Build multi-stage (5 stages)
└── docker-compose.yml      # Postgres + Redis + backend + ngrok (perfil dev)
```

### Módulos backend (`src/modules/`)

Cada módulo es una carpeta con `routes.ts` (y opcionalmente `service.ts`, `schemas.ts`),
registrada en `src/server.ts` con prefijo cuando aplica.

| Módulo | Prefijo | Auth | Propósito |
|--------|---------|------|-----------|
| `auth` | `/auth` | No | Login JWT, callback Google OAuth, emisión de token, registro |
| `businesses` | *(ninguno)* | Sí | CRUD de negocio, stats, endpoints `me`, rebuild del prompt del agente |
| `agents` | *(ninguno)* | Sí | CRUD de agentes, sync a asistentes Vapi/Retell |
| `calls` | *(ninguno)* | Sí | Registro de llamadas, transcripciones, outcomes (paginado) |
| `recordings` | *(ninguno)* | Sí | Metadatos de grabaciones, notas de revisión |
| `calendar` | `/calendar` | Sí\* | OAuth Google/Outlook, listar eventos, reservar citas |
| `bookings` | `/booking-settings` | Sí | Servicios, profesionales, capacidad de reserva |
| `billing` | `/billing` | Sí\* | Checkout Stripe, portal, resumen de suscripción, webhooks |
| `phone` | `/phone` | Sí | Estado del número Twilio, reintento manual de provisioning |
| `places` | *(ninguno)* | Sí | Autocomplete y detalles de Google Places |
| `files` | `/agents` | Sí | Subida de archivos del agente (multipart, límite 10 MB) |
| `onboarding` | *(ninguno)* | Sí | Estado de onboarding: progreso, dismiss, complete |
| `demo` | *(ninguno)* | — | Demo pública de voz |
| `voiceTools` | *(ninguno)* | — | Herramientas de voz invocadas por los orquestadores |

\* Excepto los callbacks OAuth (`/calendar/auth/*/callback`) y el webhook de Stripe (`/billing/webhook`).

### Librerías clave (`src/lib/`)

`prisma.ts` (singleton con guard de hot-reload) · `redis.ts` · `queue.ts` (`recordingQueue`,
`classifyQueue`) · `storage.ts` (R2/S3) · `stripe.ts` · `twilio.ts` · `microsoftGraph.ts` ·
`agentBootstrap.ts` (config por defecto, builders de payload Vapi/Retell, naming seguro) ·
`businessSchedule.ts` · `availability.ts` · `logUtils.ts` · `managedAgentPrompt.ts` ·
`ngrok.ts`.

### Rutas frontend (`frontend/src/app/`)

| Ruta | Propósito |
|------|-----------|
| `/` | Dashboard (protegido): llamadas, stats, setup score, subida de archivos, próximos eventos |
| `/landing` | Landing genérica de conversión |
| `/login`, `/register`, `/register/business[/niche|/services|/team|/calendar]` | Flujo de registro |
| `/auth/google/callback` | Consumo de sesión Google OAuth |
| `/barberia`, `/peluqueria`, `/fisioterapia`, `/centro-de-estetica`, `/salon-de-unas` | Landings SEO por nicho |
| `/planes` | Precios con titular sensible al ROI |
| `/checkout?plan=` | Stripe Embedded Checkout |
| `/checkout/resultado` | Polling de reconciliación post-checkout |
| `/ajustes` | Setup completo (horario, servicios, profesionales, calendario, ajustes del agente) |
| `/ajustes/facturacion` | Resumen de facturación + Stripe Customer Portal |
| `/settings` | Handler de callback de calendario (Google/Outlook) |

### Componentes clave

`SiteLanding` · `LandingHero` (+ `HeroConversation`) · `RevenueLossCalculator` ·
`PlansWithRoi` / `PlansHeadline` · `DemoVoiceCall` · `BusinessHoursEditor` ·
`AgentSettingsEditor` · `UpcomingCalendarEvents` (refresco cada 5 min) ·
`AppShell` (layout protegido: Panel, Ajustes, Facturación, logout, botón atrás en móvil).

### Frontend lib (`frontend/src/lib/`)

`api.ts` (Axios + Bearer) · `types.ts` · `plans.ts` · `niche-landings.ts` (600+ líneas de
copy SEO por nicho) · `roi-context.ts` · `seo.ts` (metadata, JSON-LD) · `format.ts` ·
`billing-navigation.ts` · `business-type.ts` (labels, keywords Places, textos de onboarding
por nicho) · `vapi.ts` · `service-templates.ts`.

## Decisiones de Diseño ya Tomadas

**Filosofía:** estética **SaaS conservadora y profesional**, pensada para negocios
tradicionales españoles (peluquerías, clínicas, barberías). **Evitar** el look genérico de
startup o app de consumo.

### Paleta de color (`frontend/src/app/globals.css`)

| Token | Hex | Uso |
|-------|-----|-----|
| `--background` | `#eef2eb` | Base del gradiente de fondo |
| `--foreground` | `#17211c` | Texto principal |
| `--surface` | `#ffffff` | Fondo de tarjetas y paneles |
| `--muted` | `#5f6a5e` | Texto secundario, descripciones (usar `.text-muted`, no el hex) |
| `--accent` | `#1e2b22` | Botones primarios, titulares, énfasis |
| `--accent-strong` | `#243026` | Hover de elementos primarios |
| `--accent-soft` | `#b8d96e` | Focus rings, highlights sutiles |
| `--success` | `#2c7334` | Estados de éxito, indicadores de conexión |
| `--warning` | `#9f7a15` | Estados de aviso |
| `--error` | `#c53030` | Errores, fallos de validación |

**Colores prohibidos:**
- `#101814` (negro puro con tinte verde) — sustituido por `#1e2b22`
- `#d6ff72` (lima neón) — sustituido por `#b8d96e`
- `#0b110e` (negro del pie) — sustituido por `#1e2b22`
- `#8b9a7f`, `#7a8774`, `#718064`, `#8e968d` — todos por debajo de 4,5:1 sobre blanco. Usa
  `#54634b` para texto secundario, `#6b756a` para placeholders, o el token `--muted`.
- Púrpura y naranja en el widget `HeroConversation`

A día de hoy quedan **cero** apariciones de cualquiera de ellos en `frontend/src`. Comprobación:
`grep -rnE '#d6ff72|214,\s*255,\s*114|#101814|16,\s*24,\s*20|#0b110e' frontend/src`

Cada nicho tiene además su propio `NicheAccent` (`strong` / `soft` / `deep`) definido en
`niche-landings.ts`, aplicado a texto, iconos, bordes, chips y degradados de esa landing.

### Clases base

| Clase | Propósito | Propiedades clave |
|-------|-----------|-------------------|
| `.panel` | Tarjeta / contenedor | `rounded-2xl`, `bg-white/95`, sombra sutil, glassmorphism |
| `.field` | Inputs de texto | `rounded-lg` (8px), `h-11`, borde `#d2dacd`, focus ring |
| `.btn-primary` | Acción primaria | `rounded-lg` (8px), `bg-[#1e2b22]`, texto blanco |
| `.btn-secondary` | Acción secundaria | `rounded-lg` (8px), fondo blanco, borde `#d2dacd` |
| `.badge-soft` | Badges de estado | `rounded-full`, `bg-[#eef6dc]`, texto `#405115` |

### Reglas de estilo

- **Border radius:** la escala es 8/12/16 px y nada más. `rounded-lg` en inputs y botones,
  `rounded-xl` en tarjetas, `rounded-2xl` en paneles. `rounded-full` solo en badges, chips,
  pastillas de navegación y botones de icono de 32–40 px. **Nunca** `rounded-3xl` ni valores
  arbitrarios tipo `rounded-[1.75rem]`.
- **Sombras:** techo de 32 px de desenfoque y 0,16 de opacidad, siempre tintadas
  `rgba(30,43,34,…)` — nunca grises ni negras. `0_8px_24px_rgba(30,43,34,0.06)` en paneles,
  `0_4px_16px_rgba(30,43,34,0.04)` en tarjetas pequeñas. Nada de `0_22px_55px` ni `0_26px_80px`.
- **Tipografía:** `font-semibold` reservado para H1/H2/H3, títulos de tarjeta, CTAs y cifras
  clave. `font-normal` para cuerpo y descripciones. El cuerpo nunca baja de 14 px; 12 px es solo
  para badges y encabezados de columna.
- **Tracking:** `-0.02em` en titulares (suelo `-0.04em`), máximo `0.12em` en labels en mayúsculas.
- **Sin eyebrows:** no pongas una etiqueta en mayúsculas encima de un titular. El titular se
  sostiene solo. Un badge sí vale cuando aporta información que el titular no da.
- **Accesibilidad:** compromiso WCAG 2.1 AA. Contraste calculado (no a ojo), controles de 44 px,
  foco siempre visible, modales con `role="dialog"` + `aria-modal` + trampa de foco.
- **Gradientes:** usar el gradiente del body (`radial-gradient` con tintes `#b8d96e` y
  `#1e2b22`) en todas las páginas. No usar fondos planos tipo `#f7f8f4`.
- **Iconos:** Lucide React dentro de contenedores `rounded-lg` / `rounded-xl` con
  `bg-[#eef6dc]` y `text-[#2c7334]`.

### Notas por componente

- **Tarjeta "Estado general" del dashboard:** fondo claro (`bg-[#f7f9f3]`) con acentos verdes,
  **no** una caja oscura.
- **Widget `HeroConversation`:** paleta verde (`#2c7334`, `#b8d96e`, `#eef6dc`) acorde al
  sitio. Nunca púrpura ni naranja.
- **Checkout:** contenedor con `min-h-[480px]` y `rounded-2xl` para evitar el colapso del
  estado vacío.
- **Páginas de auth:** clase `.panel` + gradiente del body. Nada de fondos planos.

## Constraints & Limitaciones

**Producto y negocio**
- **RGPD:** las cuentas europeas deben usar Retell (`detectVoiceOrchestrator(countryCode)`
  devuelve `retell` en Europa, `vapi` fuera). No cambies este default sin decisión explícita.
- Idioma por defecto del agente `es-ES`, zona horaria `Europe/Madrid`.
- El checkout de Stripe es **obligatorio** para completar el registro.
- `createCheckoutSession` rechaza si el negocio ya tiene suscripción activa o en trial.

**Límites técnicos duros**
- Subida de archivos: **10 MB** (`@fastify/multipart`).
- Rate limiting: 100 req/min global · 300 req/min en webhooks Vapi/Retell ·
  10/min en `/login`, `/register` y endpoints de Places · 5/min en `/register-first-user`.
- JWT: expiración de **7 días**. `JWT_SECRET` obligatorio (el servidor no arranca sin él).
- CORS restringido al origen exacto de `FRONTEND_URL`.
- Duración de servicios: **5–480 minutos**. Empleados 1–20, capacidad de reserva 1–50.
- Horario: 7 días, máximo **3 intervalos no solapados** por día, formato `HH:mm`.
- Clasificación de llamadas: `claude-haiku-4-5` con `max_tokens: 10`.
- Workers BullMQ: concurrencia 3 (`process-recording`) y 5 (`classify-call`);
  3 reintentos con backoff exponencial base 2s.
- Limpieza de llamadas zombie: cada 15 min, umbral 60 min.
- Caché Redis de configs de calendario: `vapi_config:<assistantId>`, TTL 3600s.

**Rendimiento**
- TanStack Query: `staleTime` 30s, sin reintentos, sin refetch al enfocar. Cualquier vista
  nueva debe respetar esta config en vez de inventar la suya.
- `PATCH /business/me` sincroniza todos los agentes en paralelo (`Promise.all`) para no agotar
  el timeout de la petición. No lo conviertas en secuencial.
- `UpcomingCalendarEvents` refresca cada 5 min — no bajes ese intervalo.
- `/health` comprueba Postgres, Redis, Vapi y Retell; `200` si todo sano, `503` si degradado.

**Compatibilidad / soporte**
- Móvil: `AppShell` incluye botón de "atrás" específico para móvil y las landings tienen
  menú móvil. El diseño debe ser responsive.
- La demo de voz requiere `@vapi-ai/web` y permiso de micrófono en el navegador: es
  **solo Vapi** y necesita un navegador moderno con WebRTC.
- Sin objetivo declarado de soporte para navegadores legacy ni Lighthouse: `next/core-web-vitals`
  es el único listón automatizado. **Si necesitas targets numéricos de performance o una
  matriz de navegadores concreta, hay que definirlos — no están en `AGENTS.md`.**

## Problemas UI/UX Conocidos

> El sistema visual completo vive ahora en **`DESIGN.md`** (raíz) con su sidecar
> `.impeccable/design.json`, y el contexto de producto en **`PRODUCT.md`**. El último informe de
> crítica está en `.impeccable/critique/`. En caso de conflicto manda `DESIGN.md`.

**Resueltos** en el pase de agosto de 2026 (crítica de `/landing`, 20/40 → correcciones):

1. ~~Colores prohibidos `#101814` y `#d6ff72`~~ — cero apariciones en todo `frontend/src`.
2. ~~Radios inconsistentes~~ — 21 fuera de escala eliminados; cero `rounded-3xl` y cero arbitrarios.
3. ~~Sombras excesivas~~ — las 7 que pasaban del techo bajadas a la escala del sistema.
4. ~~Tracking extremo~~ — `tracking-[0.3em]` y `[0.28em]` bajados a `0.12em`.
5. ~~Texto por debajo de AA~~ — `--muted` corregido a `#5f6a5e`; `#8b9a7f`, `#7a8774`, `#718064` y
   el placeholder `#8e968d` sustituidos.
6. ~~Micro-tipografía ilegible~~ — `HeroConversation` pasó de 8–11 px a 14 px.
7. ~~Modal de demo sin semántica~~ — `role="dialog"`, `aria-modal`, trampa de foco y bloqueo de
   scroll; los errores del SDK ya no llegan crudos a pantalla.
8. ~~Sin páginas legales~~ — `/legal/privacidad` y `/legal/aviso-legal` creadas y enlazadas.
9. ~~Enlace de salto roto~~ — apuntaba al `<main>` que lo contenía; ahora a `#contenido`.
10. ~~Menú móvil que no cerraba~~ — Escape, toque fuera y bloqueo de scroll.

**Siguen abiertos:**

- **Cajas oscuras en el dashboard:** la tarjeta "Estado general" debe ser `bg-[#f7f9f3]` con
  acentos verdes, no un bloque oscuro. Sin verificar visualmente: requiere sesión.
- **Reconexión de calendario:** cuando el refresh token caduca (`invalid_grant`), el backend
  devuelve `GOOGLE_CALENDAR_RECONNECT_REQUIRED` / `OUTLOOK_CALENDAR_RECONNECT_REQUIRED`. El
  frontend **debe** mostrar un aviso claro de reconexión. Verifica que ese estado esté cubierto.
- **Superficies autenticadas sin verificación visual:** panel, ajustes y facturación recibieron los
  tokens del sistema y pasan typecheck, lint y build, pero nadie las ha visto renderizadas porque
  hacen falta credenciales.
- **La sección "Soluciones por negocio"** queda huérfana entre la calculadora y los precios, sin
  entrada desde la navegación.
- **Datos legales pendientes:** razón social, CIF y domicilio en las dos páginas de `/legal`,
  marcados con bloques `LegalTodo` visibles a propósito.

## Lo que NO tocar

**Seguridad — crítico**
- Verificación de firmas de webhooks: HMAC-SHA256 de Vapi (`x-vapi-signature`, comparación
  timing-safe), firma de Retell (`x-retell-signature`, incluida en los endpoints de
  herramientas `/webhooks/retell/tools/:toolName`) y firma de Stripe (ruta con `rawBody: true`).
  No relajes, saltes ni "simplifiques" ninguna de estas validaciones.
- **Aislamiento multi-tenant:** toda consulta con ámbito de negocio se filtra por
  `request.user.businessId` tomado del token. **Nunca** aceptes un `businessId` del body para
  lecturas o escrituras.
- `JWT_SECRET` obligatorio y CORS bloqueado a `FRONTEND_URL`. No añadas orígenes comodín.
- `rawBody` solo está activado en las rutas de webhook de Vapi y Retell — no lo extiendas a
  rutas normales (coste de memoria).
- **Nunca** commitees `.env`. Ojo: en la raíz existen `.env` y `.env.bak` con credenciales
  reales.

**Arquitectura — respetar los límites**
- **Adaptadores como única fuente de verdad:** todas las llamadas a la API de Vapi pasan por
  `src/adapters/vapi/VapiAdapter.ts` y las de Retell por
  `src/adapters/retell/RetellAdapter.ts`. Jamás llames a esas APIs directamente desde un route
  handler.
- **Idempotencia de Stripe:** los eventos se deduplican vía la tabla `StripeWebhookEvent`. Los
  fallos de Twilio durante `provisionPhoneNumber` **no** deben hacer fallar la respuesta del
  webhook de Stripe (ese comportamiento es intencional).
- **Provisioning de teléfono:** `provisionPhoneNumber` se dispara de forma asíncrona en
  `checkout.session.completed`. Mantén la tolerancia a fallos parciales que ya cubren los tests.
- Los campos `stripeCustomerId`, `stripeSubscriptionId`, `twilioPhoneNumber*`,
  `vapiPhoneNumberId`, `retellPhoneNumberId` y `retellPhoneNumber` son **únicos** en el schema.
- Los tokens de calendario viven en `Business` (`googleRefreshToken`, `outlookRefreshToken`).
  No los muevas ni los expongas por API.
- La completitud de los pasos de onboarding se **calcula en vivo** desde los datos del negocio;
  solo `dismissedAt` y `completedAt` se persisten. No la conviertas en estado persistido.
- El default de `orchestrator` es `retell` por RGPD. No lo cambies a la ligera.

**Convenciones que no se negocian**
- Imports con extensión `.js` en rutas TypeScript (requisito de ESM):
  `import { prisma } from "../../lib/prisma.js";`.
- Prettier: `semi: true`, `trailingComma: "es5"`, `singleQuote: false`, `printWidth: 80`,
  `tabWidth: 2`.
- Alias: backend `"@/*"` → `"./*"` (relativo a `src/`); frontend `"@/*"` → `"./src/*"`.
- Logs prefijados con el módulo entre corchetes: `[Agent]`, `[Calendar]`, `[Job]`.
  `fastify.log` dentro de rutas; `console.log`/`console.error` en arranque y workers.
- Nueva variable de entorno ⇒ añadirla a `.env.example` **y** a `docker-compose.yml`
  (servicios `backend` y `backend-dev`). Sin defaults reales para secretos.
- El proyecto **sí es un repositorio git** (rama `main`). Hay historial y se puede revertir, pero
  nunca commitees `.env`, `.env.bak` ni `.env.google`: están en `.gitignore` por algo.

## Audiencia

**Usuario final (cliente de pago):** dueño o gerente de un pequeño negocio de servicios en
España — peluquería, barbería, salón de uñas, centro de estética, clínica de fisioterapia.

- Perfil: no técnico. Trabaja de cara al público y no puede coger el teléfono mientras atiende.
  Suele ser un negocio de 1 a 20 empleados.
- Dolor principal: **llamadas perdidas = citas perdidas = ingresos perdidos**. De ahí que el
  producto lleve calculadora de pérdida de ingresos (`RevenueLossCalculator`) y copy orientado
  a ROI en `/planes`.
- Expectativa: configurarlo una vez y olvidarse. Por eso el registro es guiado paso a paso
  (Places → nicho → servicios → equipo → calendario → checkout) con plantillas por nicho y la
  posibilidad de saltarse pasos y completarlos luego en `/ajustes`.
- Nivel de tolerancia: bajo a la jerga técnica y a la fricción. Nada de terminología de
  "LLM", "orquestador", "webhook" o "API" en la UI.

**Usuario secundario (quien llama):** el cliente del negocio que llama por teléfono y habla con
el agente de voz. Habla español, espera una respuesta natural y rápida, y no debería notar que
está hablando con una IA torpe. El prompt del agente incluye instrucciones explícitas para
**nunca inventar datos** y verificar siempre con `check_business_hours` y `check_availability`
antes de `book_appointment`.

## Tono & Voz

**Principio rector:** confianza tranquila y profesional. BotBook le habla a un negocio
tradicional, no a un early adopter de tecnología.

- **Idioma:** español de España, siempre. Tuteo en la UI del producto.
- **Registro:** cercano pero serio. Ni corporativo acartonado ni startup con emojis.
- **Concreto sobre abstracto:** el copy se apoya en números y resultados verificables — minutos
  incluidos, euros por minuto, citas recuperadas, ROI. Las landings por nicho llevan
  `stats` con `source` y `quotes` precisamente para respaldar las afirmaciones.
- **Enfocado al dolor, sin alarmismo:** cada nicho tiene su `painPoint` y sus `benefits` con un
  campo `result` explícito. Se nombra el problema y se cuantifica la solución; no se exagera.
- **Sin jerga de IA:** hablar de "recepcionista virtual" y "agente de voz", no de modelos,
  prompts ni orquestadores. El usuario compra una recepcionista, no una IA.
- **Voz del agente telefónico:** cálida y eficiente. Mensaje inicial por defecto:
  *"Hola, soy la recepcionista virtual. ¿En qué te ayudo?"*. El tono es configurable por negocio
  en `AgentSettingsEditor`:
  - **Tono:** `warm` / `professional` / `direct`
  - **Objetivo:** `bookings` / `customer_service` / `lead_capture`
  - **Estilo de respuesta:** `concise` / `balanced`
  - **Escalado:** `take_message` / `request_callback`
- **Personalización por nicho:** títulos, subtítulos y CTAs cambian según `businessType` vía
  `BUSINESS_TYPE_ONBOARDING_TEXTS` (`frontend/src/lib/business-type.ts`). Una peluquería y una
  clínica de fisioterapia no leen el mismo copy.
- **Errores:** mensajes en español, accionables y sin culpar al usuario. Los errores 5xx del
  backend devuelven un genérico ("Internal server error") para no filtrar internals — la UI debe
  traducir eso a algo humano.
